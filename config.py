# the source directory of all the images. Assumes it is the home directory
SOURCE_DIR = "pictures/image-gallery"

# the directory in source where the images are 
IMAGE_DIR = "images"

# the directory in source where the thumbnails are
THUMB_DIR = "thumbnails"
THUMB_SIZE = (640, 480) # in pixels

# the apache alias for the source directory
WEB_DIR_ALIAS = "image-gallery"

VALID_EXTS = [".jpg", ".jpeg"]
